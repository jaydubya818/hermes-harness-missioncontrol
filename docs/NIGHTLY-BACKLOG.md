# Nightly backlog

Standing list for the scheduled nightly maintenance run. Items land here when
they are worth doing but out of scope for the night that found them, or when
they were investigated and deliberately ruled out. Check this file before
proposing work so the same ground is not re-covered every night.

## Open

- [ ] 2026-08-21 — Gate the worker's `success` claim against the evidence it attached — the orchestrator takes `execution.success` at face value (`if (!execution.success) await failRun(...)`), and `evaluateStepPolicy()` checks *that* artifacts exist, never what they say. The test-report artifact already carries `metadata.exit_code`, so `success === (exit_code === 0)` is a free cross-check for the test kind. Not done tonight because doing it properly means deciding, per step kind, what evidence the gate is entitled to demand — that is a contract change, not a patch. See `docs/HARNESS-INVARIANTS.md` invariants 2 and 4.
- [ ] 2026-08-21 — Stop the worker spreading its whole `WorkspaceContext` into the execute-step reply — `c.json({ ..., ...workspace!, ...result, step_events })` sends the echoed `envelope` and absolute host paths (`workdir`, `repoWorkspace`, `worktreePath`) back across the service boundary, and the orchestrator's `WorkerExecution` type declares only some of them. Narrow the reply to the fields the orchestrator actually consumes. Left alone tonight because it is a response-shape change with console and test callers to check, not a one-line fix. See `docs/HARNESS-INVARIANTS.md` invariant 3.
- [ ] 2026-08-21 — Declare a Node `engines` range — nothing enforces the `.nvmrc: 22` pin; `.nvmrc` is advisory and only nvm-family tools read it, so a contributor on another major installs and builds with no signal. Deliberately not added tonight: pnpm's `engine-strict` would start failing installs on Node 24 machines, so this needs a decision on whether to warn or block first.
- [ ] 2026-08-21 — Backfill timestamps when hydrating legacy persisted runs — `hydrateState` validates run shape and calls `syncRunState`, but does not stamp a missing `run.updated_at` or step `started_at`/`completed_at`. Read models each paper over this with their own `?? ""` fallbacks, which is how the artifacts date-filter bug hid; normalizing once at hydration would remove the whole class.
- [ ] 2026-08-22 — Decide whether `retryCurrentStep` should still accept a cancelled step — the resurrection bug was closed at the orchestrator route, but `packages/workflow-engine`'s `retryCurrentStep()` still lists `"cancelled"` among its retryable prior states, unlike every other `markCurrentStep*` helper (all of which refuse `completed`/`failed`/`cancelled`). Nothing else calls it today, so the hole is closed in practice; tightening the engine too is a shared-package behaviour change and wants a deliberate call on whether the state machine is allowed to un-cancel at all.
- [ ] 2026-08-22 — Stop `loadContextBundle` returning absolute host paths — `packages/memory-runtime/src/contextLoader.ts` puts fully-resolved host paths into `files[].path`, `trace.included[].path` and `trace.excluded[].path`, and `memory-api` returns them verbatim to the console. Same class as the worker leaking `workdir`/`worktreePath` (open item above): vault-relative paths are what every consumer actually uses. Not done tonight because it changes a response shape the console and the memory-runtime tests both read.
- [ ] 2026-08-22 — The `"bus"` memory class is unreachable — `classify()` in `contextLoader.ts` falls through to `"bus"`, but the candidate list is only `profile.md`, `hot.md`, `standards.md`, `recipes.md`, and `memory-api` writes bus entries to `wiki/projects/<id>/bus.md` which is never a candidate. So the context bundle can never carry the cross-agent bus that `MemoryClass` reserves a class for. Either wire `bus.md` into the bundle (a budgeting/priority decision) or drop the dead class.
- [ ] 2026-08-23 — The per-agent contract in `config/agents/*.yaml` is loaded by nothing — `packages/memory-runtime/src/contracts.ts` defines `AgentContract` (`budget_bytes`, `reads`, `writes`, `forbidden_paths`) and exports `loadContract()`, and `config/agents/agent_demo.yaml` fills all four fields. `loadContract` has no caller anywhere in the repo, including tests. Nothing enforces any of it: `loadContextBundle` hardcodes four candidate paths and ignores `reads`, takes `budget_bytes` from the HTTP request body instead of the contract, and `closeTask` writes task-log/learned/rewrites without consulting `writes` or `forbidden_paths`. This is a governance surface in a governance product that is documentation, not policy. Deciding whether the contract becomes authoritative (and what a violation does — 403, or silently drop the path) is a design call, not a nightly patch. `loadContract` also casts `YAML.parse()` straight to `AgentContract` with no shape validation, so whoever wires it up must validate first.
- [ ] 2026-08-21 — Test the console's stateful components — `apps/harness-console/src/App.tsx` (879 lines) and `CommandPalette.tsx` have no tests. Only the pure helpers extracted into `api.ts` are covered, so filter/stream/palette wiring regressions are invisible to CI.
- [ ] 2026-08-24 — There is no CI — the repo has no `.github/` directory at all, so nothing runs `pnpm typecheck`, `pnpm test` (258 tests across all 13 workspace packages) or `pnpm build` on a push or a pull request. Every fix that has landed here, nightly or otherwise, was verified only on whoever's machine produced it, and an unmerged nightly branch has no signal at all. A ready-to-apply unified diff for `.github/workflows/ci.yml` (checkout, `pnpm/action-setup` reading `packageManager`, `setup-node` reading `.nvmrc`, `install --frozen-lockfile`, then typecheck/test/build) was produced on 2026-08-24 and verified with `git apply --check`, but the nightly token deliberately has no `workflow` scope, so it cannot be pushed by this job — it has to be applied by hand. Note for the next run: that diff was never stored anywhere in the repo (no `.github` blob exists in any ref, on any branch, at any point in history), so it was regenerated from scratch on 2026-08-25 and re-verified with `git apply --check` against the pushed `main`. Do not go looking for the 2026-08-24 copy. Regenerated again on 2026-08-28 (47 lines, `.github/workflows/ci.yml`), `git apply --check` clean against `main`, then applied to a scratch tree and the resulting YAML parsed with the workspace's own `yaml@2.9.0` to confirm all seven steps and all four `run` commands survive — `pnpm install --frozen-lockfile`, `pnpm typecheck`, `pnpm -r test`, `pnpm -r build`, the exact chain verified green that night at 279 tests / 13 of 13 projects. Delivered outside the repo as `workflow-patch-hermes-harness-missioncontrol-2026-08-28.diff`. Note for whoever regenerates it next: the first attempt had a hunk header of `@@ -0,0 +1,41 @@` against 47 lines of body, and `git apply --check` *passed* anyway while `git apply` silently truncated the file after the Typecheck step — always apply it and parse the result, never trust `--check` alone on a new-file patch.

- [ ] 2026-08-25 — `safeRelativePath` does not normalize `.` segments, so a dot-prefixed `writable_paths` entry is silently denied — `assertAllowedRepoWrite` in `apps/worker-runtime/src/index.ts` compares `safeRelativePath(rel)` against `safeRelativePath(allowedPath)`, and that helper only drops empty segments (`path.split("/").filter(Boolean).join("/")`). `"src/"` normalizes to `"src"` as intended, but `"./src"` normalizes to `"./src"`, which can never equal or prefix the `"src/..."` the relative target produces. `validateEnvelope` accepts `"./src"` happily (it resolves it against the repo root), so the envelope grants a path the write gate then refuses with `write_path_outside_repo_scope`. Not fixed tonight because it is unreachable from this build: the orchestrator is the only producer of `writable_paths` and it emits exactly `[".hermes-harness"]` or `[]` (`apps/orchestrator-api/src/index.ts` ~line 225). It is still a contract-surface gap — `ExecutionEnvelope.repo_scope.writable_paths` is a public field in `packages/contracts` — and the right fix is to normalize `.` and `..` segments once in `safeRelativePath` rather than at each comparison site. Evaluated again 2026-08-26 and left open deliberately: `safeRelativePath` (`apps/worker-runtime/src/index.ts` ~line 567) is only ever consumed by `assertAllowedRepoWrite` (~line 221), which is a write-authorization gate, so this sits on the nightly auto-merge exclusion list no matter how green the suite is — it can only ever land as a reviewed PR. The `..` half is the reason: dropping `..` segments the way the `.` fix drops `.` segments is wrong, because `"a/../../etc"` would normalize to `"a/etc"` rather than resolving, which loosens the gate instead of fixing it. Whoever takes this should decide up front whether `..` is resolved or rejected, and expect to open a PR.
- [ ] 2026-08-25 — The `actor` filter on `/api/read-models/approvals` can never match a pending approval — `toApprovalOperatorView` sets `actor: approval.resolved_by ?? "system"`, and `buildApprovalsReadModel` applies `query.actor` to the pending and history lists alike. A pending approval has no `resolved_by` by definition, so its `actor` is always the literal `"system"`: `?actor=jay` returns an empty `pending_approvals` for every operator, and `?actor=system` returns all of them regardless of who is involved. The console renders this field as "Actor:" on every queued approval, so pending rows all read "system". Left alone tonight because the fix is a decision, not a patch: either the view exposes a separate `resolved_by` and stops overloading `actor`, or `ApprovalRequest` grows a `requested_by` for the pending side. Both change a read-model response shape the console reads.

- [ ] 2026-08-26 — **`HARNESS_OPERATOR_TOKEN` is a single shared secret and the audit trail's actor is self-asserted** — one token value authorizes every route on all four services (`requireOperator` is duplicated verbatim in orchestrator-api, memory-api, eval-api and worker-runtime and does one `timingSafeEqual` against one env var). There are no roles, no per-operator identity and no way to revoke one holder. The visible consequence is in the audit trail: `POST /api/approvals/:id/respond` sets `approval.resolved_by = body.actor?.trim() || "operator"` and stamps that same string as the `actor` on the `approval.resolved`, `step.failed` and run-status events. The console supplies it from a free-text Settings field (`normalizeOperatorActor`), so it is a client-supplied label with no binding to the credential that authenticated the request. Any token holder can attribute an approve/reject to any name, and the approval history — the record of who authorized a high-risk deploy — cannot distinguish them. This is the governance surface of a governance product. Deliberately **not** attempted as a nightly: the fix is an auth redesign (per-operator credentials, or at minimum deriving `resolved_by` from the presented credential rather than the body), which changes the auth model, the `ApprovalRequest` contract and every service's startup configuration at once. Note this is a *design* finding, not a leak — see the `VITE_OPERATOR_TOKEN` entry under "Checked, not applicable", which stays closed: the browser-inlining hole is genuinely mitigated.

- [ ] 2026-08-27 — Two nightly branches are unmerged and waiting for a drain — `nightly/2026-08-26-improvements` (one commit, `916b148`, the `summarize()` out-of-range ratio fix recorded under Closed below) and `nightly/2026-08-27-improvements` (one commit, the `risk_score` coverage fix recorded under Closed below). Both are single-commit branches off `main` touching only `packages/eval-core/src/index.ts` / `scorer.ts` and their shared test file `packages/eval-core/src/index.test.ts` — so they *will* conflict with each other in that test file if drained carelessly; take 08-26 first. Recorded because the 2026-08-25 drain showed unmerged branches otherwise get re-derived from scratch every night: read these two diffs before proposing anything in `eval-core`. The 2026-08-27 run did not drain them because its push policy sent runtime-behaviour changes to a branch rather than to `main`. **Updated 2026-08-28: there are now three.** `nightly/2026-08-28-improvements` (two commits: the worker `runCmd` classification fix and the orchestrator artifact-hydration fix, both recorded under Closed below) joins them, for the same reason — that run's push policy also sent runtime changes to a branch. Drain order is now 08-26, then 08-27, then 08-28. Only the first two conflict with each other; 08-28 touches `apps/worker-runtime/src/index.ts` and `apps/orchestrator-api/src/index.ts` and their tests, and shares no file with either eval-core branch. Read all three diffs before proposing anything in `eval-core`, `worker-runtime`'s `runCmd`/`runTests`, or the orchestrator's `hydrateState`. **Correction, later the same night: there are four, and the fourth is not named `nightly/*`.** `ntf/2026-08-26-hydration-normalization` (`98f87ae` + `39c5ea7`, author `notes-to-factory`, 2026-08-26) has been sitting unmerged since 08-26 and already closes the 2026-08-21 timestamp-backfill and 2026-08-23 artifact-dedupe items — see the ⚠ correction on the first Closed entry below for the full overlap and the drain order. Every night since has missed it because the recorded convention is `git ls-remote --heads origin 'nightly/*'`, which does not match `ntf/`. **Replace that convention with `git ls-remote --heads origin` and read every head ahead of `main`.** Besides the four above, the remote also carries `feat/daily-brief-workflow`, `feat/factory-v0.1-contract-foundation`, `feat/factory-v0.1-contract-foundation-main`, `feat/humanizer-skill`, `pi-agent-review` and three `morning-review/weekly-2026-05-*` branches; none was examined this run, and any of them may hold work that a nightly is about to re-derive. Full drain order becomes `ntf/2026-08-26-hydration-normalization`, then `nightly/2026-08-26`, `nightly/2026-08-27`, `nightly/2026-08-28`.
- [ ] 2026-08-27 — **Stop the console hand-mirroring the orchestrator's step-action guards — the read model should say which actions are available.** This is a *design proposal raised in place of a third patch*, under the nightly whack-a-mole rule: two prior nightly commits have now fixed `apps/harness-console/src/App.tsx` for the same class of bug — `d6e8f7a` (Retry offered on a cancelled step, which the route had 409'd since 2026-08-22) and `aa9875d` (Execute/Mark-complete offered on non-current steps). Both were the same failure: a route guard was tightened server-side and the JSX condition that mirrors it was not. The cause is structural. `buildMissionsReadModel()`'s `run_cards` returns raw state — `status`, `current_step_id`, and per step `state`, `risk`, `blocked_reason`, `artifacts_count` — and the console re-derives every operator control from it by hand, so each of the six step/run actions carries a private copy of its route's precondition. Two of the six have already drifted. The remaining four have not drifted but are held correct only by an unpinned engine invariant, which is the same setup that produced the first two: `POST /api/runs/:id/resume-step` requires `current.state === "paused" && run.status === "paused"` while the console checks only `step.state === "paused"`, and `POST /api/runs/:id/cancel` returns `400 "no current step"` for a condition the console never tests. Both are unreachable today only because `transitionCurrentStep` is the sole writer of `run.status` and always sets `"paused"` alongside a paused step, and because `current_step_index` never advances past the last step — neither fact is asserted anywhere, and hydration accepts a persisted run that violates both. The durable fix is to compute the guard once, on the server, next to the route it governs: emit a per-step `available_actions` (and a per-run equivalent) in the run-card read model and have the console render exactly what it is handed. That deletes six mirrored predicates and makes the next route tightening impossible to forget. Not attempted as a nightly: it adds a field to a read-model response the console reads, and it needs a decision on whether `available_actions` is advisory (the route still enforces) or authoritative, and on whether the two `api.ts` predicates extracted by the prior two fixes (`isStepRetryable`, `isCurrentStepActionable`) stay as the client-side fallback or are deleted. Until it lands, treat any further "console offers a control the route refuses" report as evidence for this item rather than patching a seventh predicate.

## Closed

- [x] 2026-08-23 → 2026-08-28 — Artifact dedupe matched on `undefined` for legacy persisted artifacts, and a step persisted without an `artifacts` array 500'd every read model — both closed at hydration, which is where the 2026-08-23 entry asked for them ("normalize at hydration rather than adding a guard per read site"). `rehydrateRecords` validated only `run_id` and that `steps` is an array, so two legacy shapes reached the read models intact. (1) A step with no `artifacts` array: every read model does `step.artifacts.length` and `attachArtifact` does `step.artifacts.some`, so such a run 500s all of them permanently — `ensureLoaded()` runs on every request, so there is no way back short of hand-editing the state file, the same failure mode as the `null`-state and run-without-steps guards. Reproduced: `GET /api/read-models/missions` answered **500** for a persisted run whose single step omitted the array. (2) An artifact with no `artifact_id`: `POST /api/runs/:id/artifacts` looks up `step.artifacts.find((item) => item.artifact_id === body.artifact_id)` *before* assigning an id, so the first POST that also omits `artifact_id` matched the legacy entry on `undefined === undefined` and answered **200 with the old artifact** instead of creating the new one — the operator's artifact silently dropped. Reproduced: the POST returned 200 where 201 is required and the artifacts read model kept one artifact where two were attached. The console's "Add diff artifact" button posts exactly that shape (`{ step_id, type: "diff", content }`, no `artifact_id`), so this is an operator-visible data loss, not just a corrupt-state guard. `normalizeRunArtifacts()` now defaults a missing array, drops non-object entries (matching how `rehydrateRecords` treats a non-object record) and stamps `makeId("art")` on an artifact that has no id. Both live write paths already stamp an id, so nothing this build produces changes shape. Two regression tests, both confirmed failing against the pre-fix source ("expected 500 to be 200", "expected 200 to be 201"). Test count 277 → 279 (orchestrator-api 90 → 92). On `nightly/2026-08-28-improvements`.

  **⚠ Correction, same night, after the push — this is a partial duplicate and the drain must know it.** `ntf/2026-08-26-hydration-normalization` (two commits by `notes-to-factory`, `98f87ae` + `39c5ea7`, dated 2026-08-26, still unmerged) already closes this item *and* the 2026-08-21 timestamp-backfill item, with a `normalizePersistedRun()` that stamps `updated_at` → `created_at` → hydration time, backfills `started_at`/`completed_at` per step state, and mints `makeId("art")` for an id-less artifact — the same helper and the same conclusion the 2026-08-28 work reached independently. The 2026-08-28 run did not see it because it followed the recorded convention and listed `nightly/*` only; that branch uses an `ntf/` prefix, and the backlog on `main` never learned about it because its docs commit lives on the branch too. **Every future run must list *all* remote heads (`git ls-remote --heads origin`), not the `nightly/*` glob** — the repo currently carries nine non-`nightly` branches ahead of `main`. At drain time: take `ntf/2026-08-26-hydration-normalization` **first**; it and `nightly/2026-08-28-improvements` both edit the run-rehydrate callback in `hydrateState` and *will* conflict. Then keep only the half that branch deliberately declined — the missing-`artifacts`-array default. Its comment reads "a step persisted without an artifacts array already survives hydration today; dropping the whole run over it here would be a new behaviour", which is true about the *run*, but the read models do not survive it: `GET /api/read-models/missions` was reproduced answering **500** for exactly that run, permanently, because `ensureLoaded()` runs per request. The artifact-id half of the 2026-08-28 commit is redundant against `ntf/…` and should be dropped in the conflict resolution; its two regression tests are still worth keeping, since that branch pins the id behaviour but nothing pins the missing-array behaviour. Also note the `ntf/` branch's own caveat: it retires an assertion pinning the 2026-08-21 date-filter fix (its normalization makes `inDateRange`'s timestampless branch unreachable through a persisted run), which is why it was raised as a PR rather than merged.

- [x] 2026-08-28 → 2026-08-28 — `runCmd` reported a killed test process as a timeout and an over-chatty one as a missing runner — `runCmd` classifies *why* a spawned command did not finish normally, and `runTests` turns that classification straight into the operator-facing summary and into `metadata.failure_kind` on the test-report artifact. Two cases reachable in ordinary operation landed in the wrong bucket. **maxBuffer overflow:** a suite that writes more than the 25 MiB cap makes Node kill the child and reject with a `RangeError` whose `code` is the *string* `"ERR_CHILD_PROCESS_STDIO_MAXBUFFER"`; the classifier's last test is `typeof code !== "number"`, so a string code fell through to `"spawn_failed"` and the step reported **"Test runner could not be started"** for a runner that had started and run the whole suite — an install problem that does not exist. **A child killed by a signal this worker did not send** (the OS OOM killer, or a runner crashing on SIGSEGV/SIGABRT) arrives with `killed: false` and a `signal`, and the old `killed === true || signal` test called it `"timed_out"` — **"Test command timed out after 300s"**. For the test step that label was never right: `docs/HARNESS-INVARIANTS.md` already records that the envelope race aborts before `runCmd`'s own timeout can fire, so `killed === true` is unreachable there and an external signal was the *only* thing that could ever produce a timeout summary. Both confirmed on Node 24.18.1 with a throwaway probe reading `name`/`code`/`killed`/`signal` off the rejection for each of six cases (timeout, maxBuffer, self-SIGKILL, plain non-zero exit, ENOENT, abort). `failure_kind` gains `"output_exceeded"` and `"killed_by_signal"`, `CommandResult` gains `signal` so the summary can name it, and the maxBuffer test runs before the `killed` test because some Node releases also flag that child as killed. Confidence is untouched — any classified failure already scores 0.1. Both regression tests confirmed failing against the pre-fix source ("expected 'spawn_failed' to be 'output_exceeded'", "expected 'timed_out' to be 'killed_by_signal'"). Test count 275 → 277 (worker-runtime 41 → 43). On `nightly/2026-08-28-improvements`.

- [x] 2026-08-27 → 2026-08-27 — `risk_score` credited approvals on unrelated steps as high-risk coverage — `scoreRun()` documents `risk_score` as "Approval coverage of high-risk steps (0-1)", but its numerator was `approvedCount`, every approved approval in the run regardless of which step it settled. Approvals are not raised for high-risk steps alone: `evaluateStepPolicy` also requests one for any step whose worker confidence is below 0.6, whatever its risk (`policy_id: "approval-low-confidence"`), so a plan step approved through the low-confidence gate counted as coverage for a high-risk step nobody had approved. Reproduced on a failed bugfix run holding exactly two approvals — `plan` (low risk) approved and `deploy` (the run's only high-risk step) **rejected**: `scoreRun` returned `risk_score: 1.0`, a perfect high-risk approval-coverage score for a run whose only high-risk step was explicitly refused. Reachable through ordinary operation, not just corrupt state, and the falsified number is persisted into `EvalRecord` and averaged into `average_risk_score`, so it outlives the run — this is the governance metric of a governance product. The scorer already receives the orchestrator's full `Approval` records, which carry `step_id` (both request sites set `step_id: step.step_id`), so the fix reads it: `ApprovalSummary` gains an optional `step_id` and the numerator counts only approved approvals whose step is one of the high-risk steps. Denominator unchanged; `approval_count` deliberately untouched, since it is the run's count of approved approvals and not a coverage metric. An approval with no `step_id` cannot be shown to cover a high-risk step so it does not count — the same rule `inDateRange` already applies to a timestampless record. Two of the three regression tests confirmed failing against the pre-fix source (`risk_score: 1.0` where `0` is required). Test count 275 → 278. On `nightly/2026-08-27-improvements`.

- [x] 2026-08-26 → 2026-08-26 — The console offered "Execute current step" and "Mark step complete" on non-current steps — `apps/harness-console/src/App.tsx` rendered both for any step with `state === "running"`, while the sibling Interrupt/Resume/Retry/Cancel-step controls immediately below were already wrapped in `{step.step_id === run.current_step_id && ...}`. The two buttons fail differently and both fail badly: `executeCurrent(run.run_id)` posts to `/api/runs/:id/execute-current`, which takes only a run_id and ignores the step the button was rendered under, so it *silently dispatches a different step than the operator pointed at*; `completeStep(run.run_id, step.step_id)` posts to `/api/runs/:id/steps/:stepId/complete`, whose guard is `if (!step || !current || current.step_id !== c.req.param("stepId")) return 409 "step is not current runnable step"`, so its only possible outcome was an error toast — the same shape as the 2026-08-25 Retry finding, where the server side of a fix landed and the client never stopped offering the control. Fixed the way that finding was: the predicate moved into `api.ts` as `isCurrentStepActionable(step, run)` rather than staying an inline JSX condition, because App.tsx still has no test coverage (see the open item above) and an inline literal cannot be pinned in CI. The predicate also checks `step_id` is truthy before comparing — two absent ids must not compare equal into a match, the same `undefined === undefined` trap the orchestrator's artifact dedupe fell into. Four regression tests in `api.test.ts`, confirmed failing against the pre-change condition (`step.state === "running"` alone): the non-current running step returned `true` where `false` is required, as did a step with no id against a run with no current step. Test count 271 → 275. Note the 2026-08-26 entry predicted this was unreachable except through persisted state, and that remains true — `current_step_index` is only ever incremented — so this is a defence-in-depth fix, not an observed incident.
- [x] 2026-08-26 → 2026-08-26 — `summarize()` reported eval ratios that no scorer can produce, from persisted state — the 2026-08-23 fix bounded `confidence`, `efficiency_score` and `risk_score` at 1 on `POST /api/evals`, but only for records arriving after it shipped. `summarize()` in `packages/eval-core` is the read path, and its existing defence — documented in the function itself as protecting against a state file "written by an older build (or hand-edited)" — checked only `Number.isFinite`. A persisted `confidence: 500` is perfectly finite, so it went straight into `average_confidence`. Reproduced with a state file holding one record (`confidence: 500`, `efficiency_score: 42`, `risk_score: 7`): `GET /api/evals` returned `average_confidence: 500`. Reachable on any deployment whose `data/eval-state.json` predates the route guard — i.e. the upgrade path for every existing install, which is exactly why the write-side fix alone was not enough. Out-of-range ratios are now ignored, the same treatment the function already gives NaN and non-numbers, so they are excluded from the average rather than dragging it; clamping was rejected because `500 -> 1` invents a value and reports a corrupt record as a maximally-confident one. Unbounded fields (cost, counts, duration) are deliberately untouched — the route does not bound them either. Regression test confirmed failing against the pre-fix source (`average_confidence: 250.45`). Same shape as the 2026-08-25 Retry finding: the server side of a fix landed and the read side never did. On `nightly/2026-08-26-improvements`.
- [x] 2026-08-25 → 2026-08-25 — The three unmerged nightly branches are drained. `nightly/2026-08-23-improvements` (eval ratio-field validation), `nightly/2026-08-24-improvements` (blank-`HARNESS_OPERATOR_TOKEN` startup guard, the auth-fails-open fix) and `nightly/2026-08-25-improvements` (orchestrator hydration guard, console Retry fix) were merged into `main` oldest-first with `--no-ff` merge commits (`9ab63d7`, `d8b44ba`, `768a894`). No merge produced a conflict, but the "they touch different files" luck had already run out: 08-24 and 08-23 both edit `apps/eval-api/src/index.ts` and its test, and 08-25 and 08-24 both edit `apps/orchestrator-api/src/index.ts` and its test — all four auto-merged. Full `pnpm install --frozen-lockfile` → `pnpm typecheck` → `pnpm test` → `pnpm build` was re-run after every single merge, plus a fresh-clone gate on the pushed `main`: all green, 13/13 workspace projects each time. Test count 258 → 259 → 267 → 271. All three branches were then deleted from the remote, each proved contained first with `git merge-base --is-ancestor <head> origin/main`. Verified on Node 24.18.1, not the `.nvmrc` pin of 22 (see the 2026-08-24 entry below on the broken Homebrew node@22).
- [x] 2026-08-25 → 2026-08-25 — A persisted state file holding a valid-but-wrong-shaped JSON value bricked orchestrator-api permanently — `loadJsonFile` only falls back when the file is missing or unparseable, so a state file containing `null` came back as `null` and `hydrateState`'s first read of `loaded.missions` threw. `ensureLoaded()` runs on every request, so the service answered 500 to everything from then on, with no way back short of deleting the state file by hand — precisely the failure mode the per-record `rehydrateRecords` guards immediately below it exist to prevent. Reproduced: `null` in `ORCHESTRATOR_STATE_FILE` → `GET /api/missions` 500; `[]` → 200. `eval-api` and the worker's bootstrap cache both already guard this exact case with a named comment; the orchestrator, which has the most elaborate hydration hardening of the three, was the one that did not. Hydration now warns and starts from empty state when the persisted value is not a plain object. Regression test covers `null`, `[]`, `42` and `"state"`, confirmed failing against the pre-fix source. On `nightly/2026-08-25-improvements`.
- [x] 2026-08-25 → 2026-08-25 — The console offered a Retry button that the orchestrator has 409'd since 2026-08-22 — `apps/harness-console/src/App.tsx` rendered Retry for any step in `["paused", "failed", "blocked", "awaiting_approval", "cancelled"]`, but the `retry-step` route's guard (tightened when the resurrection bug was closed) accepts only the first four and answers `409 current step not retryable` for a cancelled step. The operator control's only possible outcome was an error toast. This is the second half of the 2026-08-22 fix that was never applied: the server stopped accepting the call, the client never stopped offering it. The predicate moved into `api.ts` as `RETRYABLE_STEP_STATES` / `isStepRetryable` — App.tsx has no test coverage at all (see the open item below), and an inline array literal in JSX cannot be pinned against the route it mirrors. Both regression tests confirmed failing with `"cancelled"` still in the list. On `nightly/2026-08-25-improvements`.
- [x] 2026-08-24 → 2026-08-24 — A blank `HARNESS_OPERATOR_TOKEN` silently disabled operator auth on all four services — `requireOperator()` opens with `if (!operatorToken) return null;`, so an empty-string value was indistinguishable from an unset one. Unset is the documented local default and correctly means "auth off"; set-but-blank is a deployment that meant to configure auth and got an empty value from an unset shell variable or a missing secret key, and it failed open with no signal. Reproduced on eval-api with `HARNESS_OPERATOR_TOKEN=""`: unauthenticated `GET /api/evals` returned 200 and unauthenticated `POST /api/evals` returned **201**, creating a record; any non-blank token 401s both. The console half already rejected this — `dev:console:auth` uses `${VITE_OPERATOR_TOKEN:?...}`, whose colon form fails on unset *and* empty — so the four backends were the inconsistent side. Each service now calls `assertOperatorTokenUsable()` inside its existing `if (!process.env.VITEST)` startup block and throws before `serve()` when the variable is present but trims to empty. The guard is duplicated per service to match the existing duplication of `requireOperator` and the startup block, rather than introducing a shared auth module (eval-api and worker-runtime do not depend on `shared-types`). Regression tests in all four services, each confirmed failing against the pre-fix source. On `nightly/2026-08-24-improvements`.
- [x] 2026-08-23 → 2026-08-23 — `POST /api/evals` accepted ratio scoring fields above 1 — `confidence`, `efficiency_score` and `risk_score` are documented 0-1 ratios that `summarize()` averages straight into `average_confidence` & co, but the route only checked them for non-negative finiteness. A record posted with `confidence: 500` was stored and the listing reported `average_confidence: 500`, a number no scorer here can produce — `scoreRun()` clamps its own output and `policy-engine` already clamps the worker-reported confidence for exactly this reason. Route now 400s on any of the three above 1; counts and durations stay unbounded. Regression test confirmed failing against the pre-fix source. On `nightly/2026-08-23-improvements`.
- [x] 2026-08-22 → 2026-08-22 — `retry-step` resurrected a cancelled run — `POST /api/runs/:id/retry-step` gated on the step state and accepted `"cancelled"`, so `POST /cancel` followed by `POST /retry-step` returned 200 and flipped `run.status` back to `running`. Cancel is a close-out: it has already recorded the eval and released the worktree and branch, and `/execute-current` refuses cancelled runs outright — retry-step was a one-call bypass. Route now 409s; regression test pins it. On `nightly/2026-08-22-improvements`.
- [x] 2026-08-21 → 2026-08-21 — `@types/node` on the `^24` line against a Node 22 runtime — the types described APIs the pinned runtime lacks, so `tsc` green-lit calls that throw. Pinned to `^22`; verified by a probe using `URLPattern` (a global only from Node 24) that typechecks under `@types/node` 24 and fails under 22.
- [x] 2026-08-21 → 2026-08-21 — Date filter hid records it was never given a range for — `inDateRange()` returned false for a timestampless record even with no `from`/`to`, dropping it from the unfiltered read model. Returns true when neither bound is supplied; regression test pins both halves.

## Checked, not applicable

- 2026-08-28 — The August 2026 Next.js critical releases and the `sharp`/libheif AVIF advisory — neither reaches this repo, checked directly rather than inherited. There is no `next` dependency in the root manifest or in any of the thirteen workspace manifests, no `next.config.*`, no `app/` or `pages/` directory, and no `sharp` anywhere in `pnpm-lock.yaml` (the advisory chain is unauthenticated AVIF → libheif → `sharp` → Next's Image Optimization endpoint; this console is plain Vite + React 19 and has no image-optimization path at all). Same conclusion as the 2026-08-25 and 2026-08-27 entries below, re-derived from the manifests this run rather than copied forward. Do not re-check before a `next` or `sharp` dependency actually appears.
- 2026-08-28 — ESLint 9 end-of-life / the ESLint 10 migration — does not apply, for the reason already recorded on 2026-08-21: there is no `lint` script and no ESLint config anywhere in the workspace, root or per-package, and no `eslint` dependency in any of the thirteen manifests. There is nothing on an ESLint version to migrate. Verification level V1 for this repo therefore stays "typecheck only". Recorded because the question arrives from outside as a fleet-wide item and the answer here is structural, not a version pin.
- 2026-08-28 — Node 20 EOL and the Node 22 support window — still not a finding, fourth time recorded. `.nvmrc` is `22` (Maintenance LTS to 2027-04-30), the root manifest pins `@types/node ^22.19.2`, and nothing declares a Node 20 floor. The related `engines` item stays Open and was deliberately not touched: it is a warn-vs-block decision, not a patch, and the 2026-08-21 entry already says so. Do not re-raise the EOL half before 2027.
- 2026-08-28 — Committed secrets and fixture PII — clean, seventh consecutive night. Scanned all tracked content for GitHub (`gh[pousr]_`, `github_pat_`), OpenAI (`sk-`), AWS (`AKIA`) and Slack (`xox[baprs]-`) token shapes, PEM private-key headers, and `api_key`/`secret`/`password`/`token` assigned to a 12+ character literal: zero hits outside this backlog file's own prose. `git ls-files` shows no tracked `.env*`, no tracked `data/`, no `.pem`, no `.key`. `vault/agentic-kb/wiki/` is unchanged at six one-line placeholder files (`agent_demo` profile/hot/learned/task-log, `proj_demo` standards/recipes) totalling 328 bytes — no tokens, no keys, no phone numbers, no platform conversation content. Every `HARNESS_OPERATOR_TOKEN` / `VITE_OPERATOR_TOKEN` occurrence in source is an `env` read, a startup guard, a comment or an operator-facing hint string; no value is committed anywhere.
- 2026-08-28 — Draining the three unmerged nightly branches — deliberately not done this run, and this is a policy limit rather than a technical one. Merging `nightly/2026-08-26/-27/-28` into `main` is a runtime-behaviour change to `main`, and the 2026-08-28 run's push policy sends runtime-behaviour changes to a branch. The drain is recorded as an Open item above with the order and conflict analysis, so whoever has the mandate can do it without re-deriving any of it.

- 2026-08-27 — `AUDIT_EVENT_TITLES` is complete and its bare index lookup is not exploitable — checked because `buildAuditReadModel` does `AUDIT_EVENT_TITLES[event.type]` on a plain object literal, which is the exact prototype-lookup hole `createWorkflowRun` was fixed for (`Object.hasOwn(WORKFLOW_LIBRARY, workflow_id)`, with a comment). It does not reach here: every stored event has passed `normalizeEventType`, which throws unless the type is in `CANONICAL_EVENT_TYPE_SET`, and hydration drops the events that throw — so `event.type` can only ever be one of the 33 canonical dotted types, never `"toString"` or `"constructor"`. Also diffed the two lists mechanically: `CANONICAL_EVENT_TYPES` (33) and the map's keys (33) match exactly, none missing, none extra. That second half matters independently — a canonical type absent from the map would fall to the `{ kind: "event" }` default and silently drop out of the audit read model's `?kind=step` filter. Re-run the diff rather than re-deriving it if either list changes.
- 2026-08-27 — Node 22 EOL, MCP spec 2026-07-28, the Next.js releases, and the Anthropic SDK — all four re-checked this run and all four still do not apply, unchanged from the entries below. `.nvmrc` is still `22` (Maintenance LTS to 2027-04-30), so no EOL finding — recorded explicitly so future runs stop re-checking it. No `@modelcontextprotocol/*` dependency and no `modelcontextprotocol` / `Mcp-Session-Id` string in any of the thirteen workspace manifests or any source file, so the 2026-07-28 stateless-core, OAuth/OIDC-hardening and versioned Apps/Tasks extension changes have nothing to bind to. No `next` dependency and no `next.config.*` / `app/` / `pages/` directory, so the 2026-08-25 critical release (GHSA-2xp9-vwfh-vxw4, the unauthenticated Image-Optimization RCE via libheif/`sharp` on attacker-controlled AVIF, patched in 16.3.3 / 15.5.24, plus CVE-2026-75604) does not reach this repo — the console is plain Vite + React 19 and has no image-optimization path at all. No `@anthropic-ai/*` dependency either.
- 2026-08-27 — Per-package dependency surface re-read from the manifests, not the root — clean, no advisories against any pinned line. Read all thirteen `apps/*/package.json` and `packages/*/package.json` directly: `hono ^4.13.2`, `@hono/node-server ^2.1.1`, `react`/`react-dom ^19.2.8`, `@types/react ^19.2.18`, `@types/react-dom ^19.2.4`, `vite ^7.3.6`, `@vitejs/plugin-react ^5.2.0`, `swr ^2.5.1`, `yaml ^2.9.0`, `tsx ^4.23.12`, `openapi-typescript ^7.13.0`, plus the root's `@types/node ^22.19.2` / `typescript ^5.9.3` / `vitest ^3.2.7`. Every workspace-internal dependency is `workspace:*`. `pnpm-workspace.yaml` still carries `minimumReleaseAge: 4320` and the forced js-yaml / nanoid overrides, and esbuild still resolves to a single `0.28.2` (past GHSA-67mh-4wv8-2f99). Same conclusion as 2026-08-22; the point of re-recording is that this pass read the per-package manifests specifically, since a root-only scan cannot see them.
- 2026-08-27 — Committed secrets and message-fixture PII — clean, sixth consecutive night. Scanned tracked content for OpenAI (`sk-`), GitHub (`ghp_`, `github_pat_`), Slack (`xox[baprs]-`) and AWS (`AKIA`) token shapes, PEM private-key headers, and `api_key`/`secret`/`password`/`token` assigned to a 12+ character string literal. The only hit anywhere is `apps/worker-runtime/src/index.test.ts:274`, `SSH_PRIVATE_KEY: "-----BEGIN"` — a fixture asserting that the child-env credential filter strips it, i.e. the test for the protection, not a key. `git ls-files` shows no tracked `.env*`, no tracked `data/`, no `.pem` and no `.key`. `vault/` re-verified: six tracked one-line markdown placeholders under `vault/agentic-kb/wiki/` for `agent_demo` (profile, hot, learned, task-log) and `proj_demo` (standards, recipes), 328 bytes total, no tokens, no keys, no phone numbers, no platform conversation content. `VITE_OPERATOR_TOKEN` re-checked against the browser-bundling angle and the 2026-08-21 mitigation still holds in full: `vite.config.ts` aborts `vite build` while the variable is set unless `ALLOW_OPERATOR_TOKEN_IN_BUNDLE=1`, `App.tsx` prefers the `localStorage` token from Settings, and `worker-runtime`'s `SECRET_ENV_KEYS` strips both operator tokens from spawned repo commands with no allow-list escape. No default or example value anywhere is a real credential.
- 2026-08-26 — **The 2026-08-24 "never verified on Node 22" gap is now closed.** That entry recorded that every prior run verified on Node 24 because the host's Homebrew `node@22` was a broken install. This run's host Node is **22.14.0**, matching the `.nvmrc` pin exactly, and the full chain — `pnpm install --frozen-lockfile`, `pnpm typecheck`, `pnpm test`, `pnpm build`, plus the fresh-clone gate — was run on it, all green, 13/13 workspace projects. The tree is now known good on its pinned major. Nothing here needs re-checking; the "no CI" open item is still the durable fix.
- 2026-08-26 — Fuzzed `eval-api` and `worker-runtime` for 5xx — clean, and this fills the gap the 2026-08-22 entry left (that sweep covered only `orchestrator-api` and `memory-api`). `POST /api/evals` was driven with null/array/number/string/boolean/empty bodies, wrong-typed `mission_id`/`run_id`/`eval_id`/`created_at`/`approval_count`/`artifact_count`, `cost_usd: 1e400`, a negative ratio, a 5 KB id and malformed JSON (`{`, ``, `[[[`); `GET /api/evals` with `limit=abc|1e400|-1|0|99999999999999999999`, `offset=-1|abc`, `order=%00`, `outcome=%FF`; `GET /api/evals/:id` with empty, `..`, `%2e%2e%2f`, space and a 3 KB id. `worker-runtime` got the same treatment on `/api/execute-step`, `/api/cleanup-run` and `/api/abort-execution`, including envelopes with negative and 1e12 `timeout_seconds`, empty `allowed_tools`, `resource_budget: {}`, `repo_scope: null`, a repo path outside `ALLOWED_REPO_ROOT`, and traversal segments in `mission_id`/`run_id`. No 5xx anywhere. Throwaway probes deleted. All four services are therefore now covered by the AGENTS.md "malformed input must 400, never 500" rule.
- 2026-08-26 — CORS / CSRF / body-limit parity across the four services — checked and consistent, no action. All of `orchestrator-api`, `memory-api`, `eval-api` and `worker-runtime` carry the same three middlewares with identical defaults: `cors({ origin: CORS_ALLOWED_ORIGINS ?? "http://localhost:5173,http://127.0.0.1:5173" })`, a `MAX_REQUEST_BODY_BYTES` (2 MiB) Content-Length gate returning 413, and the `application/json`-required guard on POST/PUT/PATCH/DELETE returning 415 that forces a CORS preflight on state-changing requests. All four also default `HOST` to `127.0.0.1` rather than letting `@hono/node-server` bind `0.0.0.0`. Worth recording because this duplication is the repo's deliberate convention (same as `requireOperator` and `assertOperatorTokenUsable`) and looks like drift waiting to happen — it is not currently drifted.
- 2026-08-26 — The Anthropic 2026-08-20 releases (production agents with computer use, the Skills API, the Files API) — not applicable. Nothing in this workspace pins an Anthropic SDK: no `@anthropic-ai/*` dependency in the root manifest or in any of the thirteen workspace manifests, and no `anthropic` string anywhere under `apps/`, `packages/` or `config/`. Consistent with the 2026-08-23 finding that this is a control plane which never calls a model API itself. Re-raise only if an Anthropic SDK is actually added.
- 2026-08-26 — MCP spec 2026-07-28, the July 2026 Next.js advisory, and the Node 20 EOL — all three re-checked by search this run and all three still do not apply, for exactly the reasons already recorded above (2026-08-23 for MCP, 2026-08-25 for Next.js, 2026-08-21/25 for Node). Confirmed unchanged: no `@modelcontextprotocol/*` or `modelcontextprotocol` reference in any manifest or source file, no `next` dependency and no `next.config.*`/`app/`/`pages/` directory, `.nvmrc` still `22`. esbuild is still a single copy at `0.28.2` in `pnpm-lock.yaml`, well past GHSA-67mh-4wv8-2f99. Do not re-derive any of these.
- 2026-08-26 — Committed secrets and message-fixture PII — clean, fifth consecutive night. No match anywhere under `apps/`, `packages/`, `config/`, `vault/`, `docs/` or `README.md` for GitHub (`gh[pousr]_`), OpenAI (`sk-`), AWS (`AKIA`) or Slack (`xox[baprs]-`) token shapes, or for a PEM private-key header. `.env` and `data/` are both gitignored and `git ls-files` shows zero tracked files under `data/`. Checked specifically for the Hermes messaging angle — despite the repo name there are **no** committed message fixtures, phone numbers, `@user:matrix` handles or platform conversation content anywhere: this repo is the harness/mission-control plane, not the bridge itself. `vault/agentic-kb/wiki/` is unchanged at six one-line placeholder files totalling 328 bytes.
- 2026-08-21 — Node 22 EOL / `.nvmrc` bump — Node 22 is Maintenance LTS through 2027-04-30. The pin is current and needs no action. Do not re-raise before 2027.
- 2026-08-21 — `VITE_OPERATOR_TOKEN` shipped to the browser — already mitigated and not a live finding. `vite.config.ts` aborts `vite build` while the variable is set (override: `ALLOW_OPERATOR_TOKEN_IN_BUNDLE=1`), the console prefers a `localStorage` token entered in Settings, and `worker-runtime` strips both `VITE_OPERATOR_TOKEN` and `HARNESS_OPERATOR_TOKEN` from spawned repo commands with no allow-list escape.
- 2026-08-21 — Root lint script — there is no `lint` script and no ESLint config anywhere in the workspace, root or per-package. Verification level V1 for this repo therefore means typecheck only, and typecheck requires `pnpm --filter './packages/**' build` first because packages resolve through `dist`.
- 2026-08-21 — Committed secrets — clean. `data/` is gitignored and no `data/*.json` state file, `.env`, or secret-shaped string has ever been added in the repo's 258-commit history. `vault/` holds only `agent_demo` / `proj_demo` fixtures, no real PII or captured message content.
- 2026-08-22 — "Malformed input must 400, never 500" (AGENTS.md) — holds. Fuzzed every mutating route on `orchestrator-api` and `memory-api` with wrong-typed, empty, null, array and primitive bodies, plus hostile query params on all four paginated read models (`limit=abc`, `limit=1e400`, `offset=-1`, `sort=%00`, `to=2026-99-99`). No 5xx. Throwaway probes deleted; the routes are already validated field by field.
- 2026-08-22 — Per-package dependency review — clean. Every manifest under `apps/*` and `packages/*` (the root was already reviewed on 2026-08-21) pins current majors: `hono ^4.13.2`, `@hono/node-server ^2.1.1`, `react`/`react-dom ^19.2.8`, `vite ^7.3.6`, `swr ^2.5.1`, `yaml ^2.9.0`, `tsx ^4.23.12`, `openapi-typescript ^7.13.0`. No advisories against these lines. `pnpm-workspace.yaml` already carries `minimumReleaseAge: 4320` plus forced overrides for the js-yaml and nanoid transitive advisories.
- 2026-08-22 — `vault/` re-checked for credentials — still clean. Six markdown files under `vault/agentic-kb/wiki/` for `agent_demo` and `proj_demo` (profile, hot, learned, task-log, standards, recipes), all one-line placeholders. No tokens, no keys, no real PII.
- 2026-08-23 — MCP spec 2026-07-28 / the v2 client and server packages — **definitively not applicable; this supersedes the open item raised on 2026-08-22 and closes it.** This repo has no MCP surface at all: no `@modelcontextprotocol/*` dependency in the root manifest or in any of the thirteen workspace manifests, and no occurrence of `modelcontextprotocol`, `Mcp-Session-Id` or an MCP server/client construction anywhere under `apps/` or `packages/`. Every service boundary here is plain Hono HTTP with bearer-token auth, described by `packages/contracts`. The 2026-07-28 changes that would otherwise matter — stateless protocol core, header-based routing, `Mcp-Session-Id` removed, `initialize` handshake removed, hardened OAuth/OIDC — have nothing to bind to. Worth recording precisely because the naming invites the assumption: MCP v2 does **not** ship as `@modelcontextprotocol/sdk@2`; it is two new packages, `@modelcontextprotocol/server` and `@modelcontextprotocol/client`, at `2.0.0-beta.1`, while `@modelcontextprotocol/sdk` stays on v1. None of the three is present here. Do not re-derive this; re-raise only if an MCP dependency is actually added.
- 2026-08-23 — Hardcoded / retired Claude model ids — none exist. `claude-opus-4-1-20250805` was retired from the API on 2026-08-05, so a hardcoded reference would be a live breakage rather than a style nit; there is no match for it, for any other `claude-*` model string, or for any pinned model identifier anywhere under `apps/`, `packages/` or `config/` (searched `.ts`, `.tsx`, `.json`, `.yaml`, `.yml`, `.md`). This repo is a control plane: it orchestrates step execution and never calls a model API itself, so it has no model id to pin. The only `model` matches in the tree are read-model builders.
- 2026-08-23 — `vault/` re-checked for credentials — still clean, third consecutive night. Contents unchanged: six one-line markdown placeholders under `vault/agentic-kb/wiki/` for `agent_demo` (profile, hot, learned, task-log) and `proj_demo` (standards, recipes), totalling 328 bytes. No tokens, no keys, no real conversation content or PII. Note that `vault/` is deliberately **not** in `.gitignore` and should not be — it is the checked-in demo fixture `memory-api` reads, and `HARNESS_VAULT_ROOT` points a real deployment somewhere else. Live agent memory would land outside the repo.
- 2026-08-23 — `vitest` undeclared in nine workspace manifests — not a defect to fix piecemeal. `worker-runtime`, `harness-console`, `ui-kit`, `eval-core`, `memory-runtime`, `policy-engine`, `shared-types`, `state-store` and `workflow-engine` all run `"test": "vitest run"` without declaring vitest; only `contracts`, `eval-api`, `memory-api` and `orchestrator-api` declare it. It works because pnpm puts the workspace root's `node_modules/.bin` on PATH for every workspace script, and the root declares `vitest ^3.2.7`. Since two thirds of the workspace already depends on that behaviour, it is the repo's convention rather than an oversight in one package — adding the devDependency to nine manifests is lockfile churn with no behaviour change, and adding it to one is inconsistency. Revisit only if `node-linker` changes or a package needs its own vitest version.
- 2026-08-21 — Operator token in the SSE stream URL — `/api/events/stream?token=...` is a deliberate, documented tradeoff (`EventSource` cannot set an `Authorization` header) and the query token is compared with `timingSafeEqual` like the header path. Worth knowing that the token reaches the access log of any reverse proxy in front of the orchestrator, but it is not a defect to fix here.
- 2026-08-24 — MCP spec 2026-07-28, hardcoded Claude model ids, and the Node 22 EOL date — all three re-checked and all three still do not apply; see the 2026-08-23 and 2026-08-21 entries above for the full reasoning, which is unchanged. Re-confirmed by search this run: no `modelcontextprotocol` / `Mcp-Session-Id` / MCP server or client construction anywhere in the tree, and no `claude-*`, `gpt-*`, `sonnet`, `opus` or `haiku` string under `apps/`, `packages/` or `config/` — including no reference to `claude-opus-5` (shipped 2026-07-24), which this control plane has no reason to carry since it never calls a model API itself. `.nvmrc` is still `22` (Maintenance LTS to 2027-04-30), so the Node 20 EOL of 2026-04-30 does not reach this repo.
- 2026-08-24 — `vitest ^3.2.7` against the fleet's vitest 4 repos — version drift, not a defect, and deliberately not bumped. There is no advisory against the 3.2 line and no behaviour here depends on a v4 feature; a major bump across thirteen packages that all inherit vitest from the workspace root (see the 2026-08-23 entry on undeclared vitest) is its own piece of work with its own verification, not a nightly side effect. Revisit when something actually needs v4.
- 2026-08-24 — Verification on the pinned Node 22 runtime — could not be done and the gap is worth recording. The host's only Node 22 (`/opt/homebrew/opt/node@22`, 22.22.1_1) is a broken install: it aborts at load with `Library not loaded: libsimdjson.30.dylib`. Everything this run verified — baseline and post-change typecheck, test and build, plus the fresh-clone gate — ran on Node 24.18.1, one major above the `.nvmrc` pin. Baseline and post-change were measured on the same runtime, so the comparison holds, but nothing here has been exercised on 22. This is exactly the hole the "no CI" open item would close, since the proposed workflow reads `.nvmrc`.
- 2026-08-25 — esbuild GHSA-67mh-4wv8-2f99 (dev server responds to any origin) — does not apply. The advisory covers esbuild ≤ 0.24.2; `pnpm-lock.yaml` resolves a single esbuild at **0.28.2** (one copy, pulled in by `vite ^7.3.6` for the console), well past the fix. Recorded either way so the next run does not re-derive it: check `grep -n "^  esbuild@" pnpm-lock.yaml` before re-raising.
- 2026-08-25 — Node 20 end-of-life (2026-04-30) — does not reach this repo. `.nvmrc` is `22` (Maintenance LTS to 2027-04-30), the root manifest pins `@types/node ^22`, and nothing here declares a Node 20 floor. Same conclusion as the 2026-08-21 entry above; re-confirmed, unchanged.
- 2026-08-25 — The July 2026 Next.js security release (9 CVEs) — does not apply. There is no Next.js dependency in the root manifest or in any of the thirteen workspace manifests, and no `next.config.*`, `app/` or `pages/` directory anywhere. The console is plain Vite + React 19.
- 2026-08-25 — `vitest ^3.2.7` while sibling repos run vitest 4 — re-checked, still deliberately not bumped; the 2026-08-24 entry's reasoning is unchanged (no advisory against the 3.2 line, nothing here uses a v4 feature, and a major across thirteen packages that all inherit vitest from the workspace root is its own piece of work). Do not raise again as a security item.
- 2026-08-25 — Committed secrets and vault PII — clean, fourth consecutive night. No `.env`, no `data/*.json` (gitignored), and no token-shaped literal anywhere under `apps/`, `packages/`, `config/` or `vault/`. `vault/agentic-kb/wiki/` still holds only the six one-line `agent_demo` / `proj_demo` placeholder files. Worth noting as configuration rather than a secret: `ALLOWED_REPO_ROOT` and the console's repo-path input both default to the literal `/Users/jaywest/projects...`, a developer path baked into `apps/worker-runtime/src/index.ts`, `apps/orchestrator-api/src/index.ts` and `apps/harness-console/src/App.tsx`. It leaks nothing (the `/health` echo of it was already removed) and every site is env-overridable, so it is a portability wart, not a finding.
